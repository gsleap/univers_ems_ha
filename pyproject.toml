[project]
dependencies = ["aiohttp", "cryptography"]
name = "univers-ems"
version = "0.0.5"
description = "A custom Home Assistant integration for the Univers EMS solar monitoring app"
readme = "README.md"
requires-python = ">=3.12"
